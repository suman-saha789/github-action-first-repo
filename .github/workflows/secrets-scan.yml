#API Keys, sensitive data, credentials
#GitLeaks
name: Secrets Scan

on:
    workflow_call:

jobs:
    secret-scan:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
              w

            - name: run Gitleaks scaner
              uses: gitleaks/gitleaks-action@v2
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} #Github Actions manages these token.no need to generate and configure
                
